{
  "name": "gemini-proxy",
  "version": "1.0.0",
  "description": "OpenAI-compatible proxy that forwards to Google Gemini, keeping the API key server-side. Lets Show Its Work use a real LLM with zero client config.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.7.0",
    "node-fetch": "^2.7.0"
  }
}
