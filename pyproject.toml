[project]
name = "show-its-work"
version = "0.1.0"
dependencies = [
  "pandas>=2.2",
  "numpy>=1.26",
  "scikit-learn>=1.4",
  "networkx>=3.3",
  "pyyaml>=6.0",
  "pydantic>=2.7",
  "fastapi>=0.110",
  "uvicorn>=0.29"
]

[tool.vercel]
entrypoint = "web.api:app"
