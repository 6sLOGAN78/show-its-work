[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "show-its-work"
version = "0.1.0"
description = "A KPI intelligence-to-action engine that shows its work (AIC 2026, PS3)."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
